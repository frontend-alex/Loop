create image -> run the container 

- changes appear?

delete image -> create new image with snapshot -> run the container 

- automatic? 

`docker build -t myapp .`                              
`docker run --name myapp-container -p 8080:8080 myapp` 
`docker ps`                                            
`docker ps -a`                                         
`docker logs myapp-container`                          
`docker stop myapp-container`                           
`docker start myapp-container`                         
`docker rm myapp-container`                            
`docker images`                                        


`docker run -p 8080:8080 --rm -v $(pwd):/app -v /app/tmp --name [NAME] [IMAGE]`

rm = remove container when stops 
-v $(pwd):/app - mount app in the container, and edits appear immediately